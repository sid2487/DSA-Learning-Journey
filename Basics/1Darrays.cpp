#include <bits/stdc++.h>
using namespace std;

int main(){

    // Arrays: when you have a similar data types being stored multiple times.
    // 0th index will be stored at a random place in cpu but afterwrds datatypes will be stored consecutively.

    int arr[5];
    cin >> arr[0] >> arr[1] >> arr[2] >> arr[3] >> arr[4] ;
    cout << arr[2];



    return 0; 
}