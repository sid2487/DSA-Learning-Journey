#include <bits/stdc++.h>
using namespace std;

// subsequence: a contagious/non-contagious sequence, which follows the order.
// A subsequence is a sequence that is formed by deleting some(or no) characters from the original string or array without changing the order of the remaining elements.

int main()
{

    return 0;
}